# Planet Caravan Inventory Sync

Utilizes Selenium to log in and download a csv of stock from ShopKeep.


## To Do:
- Create virtual env so that `requirements.txt` doesn't show global packages (best guess right now)
- Parse the csv and send data to new store
